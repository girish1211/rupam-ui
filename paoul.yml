- name: playbook_2
  hosts: all
  gather_facts: no
  tasks:

    - name: Print the task_0
      debug:
        msg: "{{ansible_selinux_python_present}}"
      tags: amin


    - name: Print the task_1
      debug:
        msg: "web server"
      tags: arif

    - name: Print the task_2
      debug:
        msg: "db server"
      tags: gihan
